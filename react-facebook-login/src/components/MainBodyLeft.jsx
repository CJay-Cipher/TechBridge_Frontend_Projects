import React from "react";
import main_logo from "../assets/fb_logo.svg";

const MainBodyLeft = () => {
    return (
        <div class="left-side">
            <img src={main_logo} alt="" />
            <p>Facebook helps you connect and share with the people in your life</p>
        </div>
    );
};

export default MainBodyLeft;
